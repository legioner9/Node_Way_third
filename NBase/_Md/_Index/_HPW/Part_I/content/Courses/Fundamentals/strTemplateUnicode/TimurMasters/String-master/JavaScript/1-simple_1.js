'use strict';

const name = 'Marcus Aurelius';

console.log();
console.log(`name = '${name}'`);

console.log();
console.log(`typeof name = '${typeof name}'`); // 'string'
console.log(`name.length = ${name.length}`); // 15
console.log(`name[3] = '${name[3]}'`); // 'c'
console.log(`name.charAt(5) = '${name.charAt(5)}'`); // 's'
console.log(`name.charCodeAt(7) = ${name.charCodeAt(7)}`); // 65 (A)
